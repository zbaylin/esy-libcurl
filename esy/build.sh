#!/usr/bin/env bash

# Empty because we don't need to build libcurl itself.
# On macOS, it is vendored w/ XCode/Clang
# On Linux, we expect you to have installed it from your package manager
# On Windows, we vendor a DLL
